# Synthetic Defect Generation using Diffusion Models

## 1. Project Overview

We want to generate ind
* **Context:** Explicitly state the industrial or research problem: the scarcity of anomalous data (defects) compared to normal data in material inspection.
* **Objective:** Explain the goal of generating synthetic defects on healthy material images to balance datasets.
* **Current State:** Mention that the current proof-of-concept is validated on a toy dataset (MNIST), successfully demonstrating domain translation (e.g., converting a standard "8" into an anomalous "3").

## 2. Methodology

Détaille ici les concepts mathématiques et architecturaux implémentés dans ton code.

* **Base Model (DDPM):** Briefly describe the U-Net architecture (`model.py`) with its specificities: ResBlocks, Adaptive Layer Normalization (AdaLN) for time embedding projection, and the Self-Attention bottleneck.
* **Partial Noising (Image-to-Image):** Explain the logic behind `inference.py`. You inject noise into a normal image up to a specific timestep ($t = \text{start\_time}$) rather than pure Gaussian noise, then denoise it to guide it towards the anomaly distribution.
* **Parameter-Efficient Fine-Tuning (LoRA):** Explain that to avoid overfitting on the extremely small anomaly dataset, Low-Rank Adaptation matrices are injected into the U-Net's `resblock2` and `decoder` modules (`finetuning.py`).
* **Classifier-Free Guidance (CFG):** Detail how guidance is used during inference to amplify the conditioning on the anomaly distribution, contrasting the uncond and cond noise predictions.

## 3. Repository Structure

Présente l'arborescence du projet pour orienter le lecteur.

```text
├── data/
│   ├── train/ (normal / anomaly)
│   └── test/  (normal / anomaly)
├── dataloader.py    # Custom Dataset and DataLoader implementation
├── finetuning.py    # LoRA layer definition and injection logic
├── inference.py     # Reverse diffusion process with CFG and comparative visualization
├── model.py         # U-Net architecture with AdaLN and Self-Attention
├── parameters.py    # Global hyperparameters (device, time steps, LoRA rank, etc.)
└── training.py      # Base DDPM training loop with cosine noise schedule

```

## 4. Requirements & Installation

* List dependencies (PyTorch, Torchvision, Matplotlib, Pillow).
* Provide the standard command: `pip install -r requirements.txt`.

## 5. Usage Pipeline

Sépare clairement les trois étapes du cycle de vie de ton modèle.

* **Step 1: Base Model Pre-training**
* Command: `python training.py`
* Description: Trains the base U-Net on the large dataset of "normal" images. Outputs `ddpm_weights_normal.pth`.


* **Step 2: LoRA Fine-Tuning**
* Command: `python finetuning.py`
* Description: Freezes base weights, injects rank-4 LoRA matrices, and trains exclusively on the limited "anomaly" dataset. Outputs `ddpm_weights_lora.pth`.


* **Step 3: Inference and Comparison**
* Command: `python inference.py`
* Description: Takes a test image, applies partial forward diffusion, and compares three reverse generation methods: Base Model, LoRA without guidance ($w=1$), and LoRA with CFG ($w=4$).



## 6. Preliminary Results (Toy Dataset)

* Include the output plot generated by Matplotlib in `inference.py`.
* Add a brief caption explaining the 5 columns: *Original (T=0) -> Noised (T=320) -> Base (no LoRA) -> LoRA (w=1) -> LoRA+CFG (w=4)*.
* Comment strictly on the visual evidence: how CFG forces the anomaly characteristics compared to the base weights.

## 7. Future Work

* Transitioning from the MNIST toy dataset to the target material dataset.
* Hyperparameter tuning for the material domain (e.g., adjusting `start_time`, LoRA rank $r$, or the CFG scale).